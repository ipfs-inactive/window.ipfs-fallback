{
  "name": "window.ipfs-fallback",
  "version": "1.2.1",
  "description": "Get IPFS API from window.ipfs with fallback to CDN if unavailable",
  "main": "index.js",
  "scripts": {
    "test": "npm run lint && npm run test:ava",
    "test:ava": "nyc --reporter=lcov --reporter=text ava",
    "lint": "standard"
  },
  "author": "Alan Shaw",
  "license": "MIT",
  "devDependencies": {
    "ava": "^0.25.0",
    "nyc": "13.1.0",
    "standard": "^12.0.1"
  },
  "dependencies": {},
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ipfs-shipyard/window.ipfs-fallback.git"
  },
  "keywords": [
    "window.ipfs",
    "ipfs",
    "script",
    "fallback"
  ],
  "bugs": {
    "url": "https://github.com/ipfs-shipyard/window.ipfs-fallback/issues"
  },
  "homepage": "https://github.com/ipfs-shipyard/window.ipfs-fallback#readme"
}
